/**
 * 
 * @param {Array} wordArrayFunc 
 * @param {Array} arrayReverseFunc 
 * @returns {boolean}
 */

function palindrome(wordArrayFunc, arrayReverseFunc) {
    if (wordArrayFunc!== arrayReverseFunc) {
        result = true
    }

    return result;
}